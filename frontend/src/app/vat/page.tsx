import { Layout } from '@/components/Layout';
import VatReference from '@/components/VatReference';

export default function VatPage() {
  return (
    <Layout>
      <VatReference />
    </Layout>
  );
}
